import { FormatSecondPipe } from './formatSecond.pipe';

describe('FormatSecondPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatSecondPipe();
    expect(pipe).toBeTruthy();
  });
});
